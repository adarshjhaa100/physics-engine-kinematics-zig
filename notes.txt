Goal:

Build Kinematics : 1d, 2d ( possibly 3d physics engine )
Simulate based on laws on kinematics

Detailed:

    - A particle, given specific velocity, acceleration, starts moving, when "collision" with boundaries, repel
    - Primarily follow 2d kinematics laws (except collision where it's elastic)

    - <Deferred for Later> Plot the displacement, velocity, acceleration curve (maybe in a separate place or shifted window)
    Meanwhile, print the stats like:
    ( posn(both direction), v (both direction), a (both direction), seconds elapsed )


Given initial acceleration, velocity:
    - Input config file to set initial params (velocity, posn, acceleration of object, external forces - accl like gravity)
    - We can move character with input (file or script - can use this to automate)
    - Arrow key provide - ACCELERATION. If it's opposite to velocity, slow down, else acceleration adds up to velocity
    - (optional) We can move character automatically on a specific path (based on some automated algorithms by building maize like structure )


Screen UI:

- top right corner - Stats
- whole screen, particle (filled circle)
- Screen Has XY axis with actual pixel markings based on proportions
- multiple options to configure between game modes

Final product including:
    1. Cannon shooter with distance count, inital angle setter (angry birds)
    2. Side scroller with jump
    3. (redacted, not required, not relevant) all direction movement 2d like zelda/pokemon etc.
    4. Motion under free fall on earth
    5. Breakout clone


DEV PROCESS:
    - File Structure and Initial Entities (Schemas, Functions) required for this process
        Kinematics - Posn, Velocity, Acceleration, Time, M:Collision, translateParticle, Accelerate etc.
        Particle - Shape, Posn
        Arena - game setting - boundaries, mazes, enemies, physics constraints (etc.)
            Setup maze structure using bitmap matrix type for simple maze with boundaries. Autoscale and autofit to the screen resolution. Make sure it's based on specific aspect ratio e.g 4:3 so matrix in 3a:4a (a multiplier int)
            converter from bitmap to grid:
                https://tuhinshubhra.github.io/bmparray/
                https://javl.github.io/image2cpp/
        Global States - Stores Everything related to global data in appstate
        Game - Configs and Their Implementation
        (FPS, Window Size, Grids, Menus, Characters in game - need config, Scripts, Debug Outputs etc)
    - Just start placing particles, adding characteristics etc, write scripts/functions for core kinematics logic, game logic etc.
    - Start with cannon shooter

TODO:
- ( done file ) config setup
- (done) link config consts and other shareable states with appstate
- initial arena with backgroud + player gen + object gen algo ready
- breakout game with proper arena setup and physics
    Breakout : 1 player wth block break
- (next) Angry Bird
